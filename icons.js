/**
 * @hakunahq/ui/icons — Curated lucide-react icon re-exports
 *
 * Requires `lucide-react` as a peer dependency (optional for consumers
 * that don't use this subpath).
 *
 * Usage:
 *   import { IconShield, IconPlus } from '@hakunahq/ui/icons'
 */
export * from './components/Icons.jsx'
