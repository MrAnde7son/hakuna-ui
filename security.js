/**
 * @hakuna/ui/security — Security-specific components
 *
 * These components are specific to security products (hakuna, hakuna-signal)
 * and are NOT part of the main `@hakunahq/ui` entry to keep the shared
 * design system free of domain coupling.
 *
 * Import explicitly:
 *   import { BlastNode, VendorLogo } from '@hakunahq/ui/security'
 */

export { BlastNode, BlastEdge, BLAST_NODE_TYPES, BLAST_EDGE_TYPES } from './components/security/BlastRadiusIcons.jsx'
export { VendorLogo, VENDOR_CATALOG, VENDOR_CATEGORIES } from './components/security/VendorLogo.jsx'
